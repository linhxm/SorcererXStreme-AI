name: Metaphysical Lambda CD

on:
  push:
    branches: [ "main" ]
    paths:
      - "src/metaphysical/**"
      - ".github/workflows/deploy_metaphysical.yml"

jobs:
  deploy:
    name: Deploy Metaphysical Lambda
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src/metaphysical

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python (3.10)
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies & Prepare Package
        run: |
          mkdir -p package
          python -m pip install --upgrade pip
          
          if [ -f requirements.txt ]; then
            pip install \
              --platform manylinux2014_x86_64 \
              --implementation cp \
              --python-version 3.10 \
              --only-binary=:all: \
              --upgrade \
              --target package \
              -r requirements.txt
          fi
          
          # Copy code, prompts và thư viện lasotuvi
          cp lambda_function.py package/
          if [ -f prompts.py ]; then cp prompts.py package/; fi
          if [ -d lasotuvi ]; then
             cp -r lasotuvi package/
          fi

      - name: Zip Lambda package
        run: |
          cd package
          find . -type d -name "__pycache__" -exec rm -rf {} +
          zip -r ../metaphysical.zip .

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-southeast-1

      - name: Deploy to Lambda
        run: |
          aws lambda update-function-code \
            --function-name sorcererxstreme-metaphysical \
            --zip-file fileb://metaphysical.zip